#include<iostream>
using namespace std;

int main()
{
	string special, jp1, jp2, jp3;
	int n, c;
	string ticket;
	int hit[7] = { 0 };
	int sum = 0;
	cin >> special >> jp1 >> jp2 >> jp3;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> ticket;
		
		do {
			if (ticket[c] == special[c] || ticket[c] = jp1[c]);


		} while (c != 0);
	}




}